// const validateFields = require("../middlewares/validate-fields");
// const validateJwt = require("./validate-JWT");
// const validateRole = require("./validate-Role");

// module.exports = {
// 	...validateFields,
// 	...validateJwt,
// 	...validateRole,
// };

//Hay que modificar el nombre de las funciones dentro de cada archivo para que no tenga el mismo nombre que lo que exporto aca.
